import {lightTheme} from './LightTheme'
import {darkTheme} from './DarkTheme'
import useThemeSwiter from './UseThemeSwiter'
const ThemeType = {
    dark : 'dark',
    light : 'light',
    default : 'light'
  }
export {
    ThemeType,
    useThemeSwiter,
    darkTheme,
    lightTheme,
}