import {lightTheme} from './LightTheme'
import {darkTheme} from './DarkTheme'

const ThemeType = {
    dark : 'dark',
    light : 'light',
    default : 'light'
}
export {
    ThemeType,
    darkTheme,
    lightTheme,
}